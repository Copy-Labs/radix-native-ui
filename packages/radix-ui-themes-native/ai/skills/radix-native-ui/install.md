# Installation

To install Copy Native UI in a project:

npm install radix-native-ui

or

yarn add radix-native-ui

or

bun add radix-native-ui

If using Expo:

npx expo install radix-native-ui

Ensure the project uses:

React Native >= 0.72
Expo >= 49
